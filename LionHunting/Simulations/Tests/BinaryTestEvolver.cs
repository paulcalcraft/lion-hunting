using System.Linq;
using GeneticAlgorithms;

namespace LionHunting.Simulations.Tests
{
    /*[EvolverFor(typeof(BinaryTestChromosome))]
    class BinaryTestEvolver : Evolver
    {
        public BinaryTestEvolver(IGeneticProbabilityProvider probabilityProvider) : base(probabilityProvider)
        {
        }

        protected override double[] Evaluate(Population population, int randomSeed)
        {
            return Enumerable.Repeat(1d, population.Size).ToArray();
        }
    }*/
}